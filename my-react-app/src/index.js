import React from 'react'; // Create the application.
import ReactDOM from 'react-dom/client'; // Create the dom structure.
import Buttons from './buttons.js';

const root = ReactDOM.createRoot(document.getElementById('root')); // Create a root element.
root.render( // Render that element.
<>
  <Buttons />
</>
);


